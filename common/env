#!/bin/bash

BBREV=`git rev-parse HEAD`
echo "Build bot at revision $BBREV."

MACHINE_TYPE=`uname -m`
if [ ${MACHINE_TYPE} == 'x86_64' ]; then
  ARCH='x64'
else
  ARCH='x86'
fi

echo "Using architecture $ARCH (${MACHINE_TYPE})"
